# {{projectName}}

A ReasonML Bucklescript project {{#if reasonReact ~}}
with ReasonReact {{/if ~}} bootstrapped by
[this project](https://github.com/alexcardell/boilerroom).

## Getting started
1. :construction: _To be removed in future versions_ :construction:

    - Rename `package.json` `name` field from app, and all occurrences of `app`.
    - Rename modules used in the ReasonML code accordingly.

1. Install dependencies
    ```
    esy install
    ```
1. Set up dune, using `pesy`
    ```
    esy pesy
    ```
1. Write some code, run some tests.
    ```
    esy test
    ```
